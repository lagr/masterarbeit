.workflow-designer--details-bar{ ng_class: '{active: processDesigner.detailsBarVisible()}'}
  - [:start_element, :end_element].each do |activity|
    = render partial: "templates/process_designer/activity_forms/#{activity}"
