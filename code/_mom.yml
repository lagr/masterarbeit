service:
  image: rabbitmq:3
  # restart: always
  net: "backend_net"
  ports:
    - "8080:15672"
  environment:
    - "constraint:node==$TARGET_NODE"
    - RABBITMQ_DEFAULT_USER=masterarbeit
    - RABBITMQ_DEFAULT_PASS=masterarbeit
    - SERVICE_NAME=mom_service
